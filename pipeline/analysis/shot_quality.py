# Shot quality analyser — computes xG metrics and goals vs xG differential.
import logging
log = logging.getLogger(__name__)


class ShotQualityAnalyser:
    def run(self):
        """Compute xG per attempt, total xG, and goals_minus_xg across all breakdowns."""
        log.info("ShotQualityAnalyser.run() — stub, not yet implemented")
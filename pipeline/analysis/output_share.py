# Output share analyser — computes free kick dependency ratios vs peers.
import logging
log = logging.getLogger(__name__)


class OutputShareAnalyser:
    def run(self):
        """Compute free kicks as % of total output for Ronaldo and all peer groups."""
        log.info("OutputShareAnalyser.run() — stub, not yet implemented")
# Zone classifier — computes distance, angle, and zone classification for each shot.
import logging
log = logging.getLogger(__name__)


class ZoneClassifier:
    def run(self):
        """Calculate distance_m, angle_deg, zone_id, and is_preferred_zone for processed_shots."""
        log.info("ZoneClassifier.run() — stub, not yet implemented")
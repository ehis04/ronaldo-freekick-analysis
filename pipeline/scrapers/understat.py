# Understat scraper — fetches shot-level free kick data for all relevant players and seasons.
import logging
log = logging.getLogger(__name__)


class UnderstatScraper:
    def run(self):
        """Scrape Understat and load raw shots into raw_shots table."""
        log.info("UnderstatScraper.run() — stub, not yet implemented")
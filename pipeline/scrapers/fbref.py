# FBRef scraper — fetches season aggregate and match-level free kick data.
import logging
log = logging.getLogger(__name__)


class FBRefScraper:
    def run(self):
        """Scrape FBRef shooting tables and load into raw_shots and seasons tables."""
        log.info("FBRefScraper.run() — stub, not yet implemented")